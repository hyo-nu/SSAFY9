import sys;sys.stdin=open('input.txt')

def pseudo_palindrome1(Str,SP,EP):
    while SP < EP:
        if Str[SP] != Str[EP]:

def pseudo_palindrome2(Str,SP,EP):
    while SP < EP:
        if Str[SP] != Str[EP]:

    return 0

for _ in range(int(input())):
    Str = input()

